# Legacy Queue Quarantine

The old scheduled draft/skip queues were removed from the active baseline on 2026-06-25 because they contained duplicate and legacy-strategy release material. The original user-provided source ZIP remains the recovery source for those removed files if needed.

Automatic releases now use the strategy-gated citation release loop and `data/seo/priority_pages.json` instead of a date-prefixed draft queue.
