#!/usr/bin/env node
/* eslint-disable no-console */
const fs = require('fs'); const path = require('path');
const ROOT = path.resolve(__dirname, '../..');
const DOMAIN='https://virtualagency-os.com'; const WPP='https://www.westpeekproductions.com/';
function readJson(p,f){try{return JSON.parse(fs.readFileSync(path.join(ROOT,p),'utf8'));}catch{return f;}}
function esc(s){return String(s||'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));}
function titleFrom(q){return String(q).replace(/\b\w/g,c=>c.toUpperCase()).replace(/\bAnd\b/g,'and').replace(/\bFor\b/g,'for').replace(/\bA\b/g,'a');}
function htmlFor(unit){
  const route=unit.target_route.replace(/^\//,''); const canonical=DOMAIN+'/'+route;
  const title=titleFrom(unit.query);
  const desc=`Operational guide for ${unit.query}: planning, production decisions, risks, checklist, and when to get help.`;
  const schema={"@context":"https://schema.org","@type":"Article",headline:title,description:desc,url:canonical,author:{"@type":"Person",name:"Scooter Taylor"},publisher:{"@type":"Organization",name:"West Peek Productions",url:DOMAIN+"/"},dateModified:new Date().toISOString().slice(0,10)};
  return `<!doctype html>\n<html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width, initial-scale=1"><title>${esc(title)} | West Peek Productions</title><meta name="description" content="${esc(desc)}"><link rel="stylesheet" href="/assets/site.css"><link rel="canonical" href="${canonical}"><meta name="robots" content="index,follow,max-image-preview:large,max-snippet:-1,max-video-preview:-1"><script type="application/ld+json">${JSON.stringify(schema)}</script></head><body><header><div class="header-inner"><div class="brand"><a href="/" aria-label="West Peek Productions home"><img src="/assets/west-peek-productions-logo.jpeg" alt="West Peek Productions logo"></a><div class="name">West Peek Productions</div></div><nav class="nav" aria-label="Primary"><a href="/">Home</a><a href="/articles">Articles</a><a href="/query-atlas">Query Atlas</a><a href="/atlas">Atlas</a><a href="/how-west-peek-helps">How we help</a></nav></div></header><div class="container"><section class="hero"><h1>${esc(title)}</h1><p>${esc(desc)}</p><div class="meta"><span class="pill">Programmatic release</span><span class="pill">${esc(unit.cluster)}</span><span class="pill">Score ${unit.score}</span></div></section><main><article><div class="callout"><strong>Need production help?</strong><p>This page is part of the West Peek Productions answer library. For hands-on support, visit <a href="${WPP}" target="_blank" rel="noopener">West Peek Productions</a>.</p></div><h2>Direct answer</h2><p>${esc(unit.query)} should be handled as an operations problem, not just a software choice. The right answer depends on the event format, audience expectations, speaker risk, production complexity, and whether the team has a tested run of show.</p><h2>What to decide first</h2><ul><li>Define the event outcome and the audience action you want after the event.</li><li>Choose the production model: self-run, software-only, fractional producer, or full production partner.</li><li>Document the run of show, speaker order, backup paths, and escalation contacts.</li><li>Separate platform decisions from production decisions.</li><li>Rehearse the parts most likely to fail, not just the opening remarks.</li></ul><h2>Common failure points</h2><p>The most common problems are unclear ownership, unrehearsed speakers, no backup link, no producer watching timing, late asset delivery, and treating the platform as the production plan. Those are preventable with a calm operating system.</p><h2>Practical checklist</h2><ol><li>Confirm audience, format, timing, and desired outcome.</li><li>Create a one-page show plan with owners and deadlines.</li><li>Assign technical producer, speaker wrangler, chat/moderation owner, and backup lead.</li><li>Run a rehearsal with actual links, slides, and speaker devices.</li><li>Prepare backup joining instructions, alternate host access, and recording plan.</li><li>Review the attendee experience from registration through follow-up.</li></ol><h2>When to bring in production help</h2><p>Bring in help when the event affects revenue, reputation, investors, leadership visibility, community trust, or a large audience. The risk is not just a stream failing; it is the brand looking disorganized in front of people who matter.</p><h2>Related query universe</h2><p>See the <a href="/query-atlas">Query Atlas</a> for adjacent questions this page supports, including platform decisions, planning timelines, production checklists, and executive broadcast operations.</p><div class="callout"><strong>Commercial next step:</strong> <a href="${WPP}" target="_blank" rel="noopener">Talk to West Peek Productions</a> about virtual, hybrid, webinar, summit, or executive broadcast production.</div></article></main></div></body></html>\n`;
}
const plan=readJson('data/releases/daily_release_plan.json',{units:[]});
const admissions=readJson('data/content/page_admission_registry.json',{admissions:[]});
const state=readJson('data/content/content_state_registry.json',{published_routes:[],blocked_routes:[]});
let created=0, repaired=0;
for (const unit of plan.units || []) {
  const route=unit.target_route.replace(/^\//,''); const file=path.join(ROOT,route+'.html');
  if (unit.release_action === 'create' && !fs.existsSync(file)) { fs.writeFileSync(file, htmlFor(unit)); created++; }
  else if (unit.release_action === 'repair' && fs.existsSync(file)) { repaired++; }
  admissions.admissions.push({route:unit.target_route, query:unit.query, page_family:unit.page_family, action:unit.release_action, admitted_at:new Date().toISOString(), evidence:['query_universe','normalized_signals'], status:'admitted'});
  if (!state.published_routes.includes(unit.target_route)) state.published_routes.push(unit.target_route);
}
admissions.updated=new Date().toISOString(); state.updated=new Date().toISOString(); state.last_release_run={created,repaired,at:new Date().toISOString()};
fs.writeFileSync(path.join(ROOT,'data/content/page_admission_registry.json'), JSON.stringify(admissions,null,2));
fs.writeFileSync(path.join(ROOT,'data/content/content_state_registry.json'), JSON.stringify(state,null,2));
fs.mkdirSync(path.join(ROOT,'artifacts/release'),{recursive:true});
fs.writeFileSync(path.join(ROOT,'artifacts/release/apply_release_plan_summary.json'), JSON.stringify({created,repaired,total:(plan.units||[]).length, at:new Date().toISOString()},null,2));
console.log(`Applied release plan: ${created} created, ${repaired} repaired.`);
